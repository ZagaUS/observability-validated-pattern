replicaCount: 1
image:
  repository: quay.io/zagaos/sustainability-dashboard
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

route:
  subdomain: infra-dashboard

dashboard:
  enabled: true

# configmapEnv: