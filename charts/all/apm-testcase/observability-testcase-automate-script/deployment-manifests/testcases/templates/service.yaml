{{- range .Values.service }}
{{- if .enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .name | default (printf "%s-%s" (include "testcases-script-automate.fullname" $) (.serviceType | lower)) }}
  labels:
    {{- include "testcases-script-automate.labels" $ | nindent 4 }}
  namespace: {{ .Values.namespace }}
  
spec:
  type: {{ .serviceType }}
  ports:
    {{- range $.Values.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      protocol: {{ .protocol }}
      targetPort: {{ .targetPort }}
    {{- end }}
  selector:
    {{- include "testcases-script-automate.selectorLabels" $ | nindent 4 }}

---

{{- end }}
{{- end }}