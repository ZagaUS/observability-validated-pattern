edge-dashboard:
  enabled: true
  replicaCount: 1
  image:
    repository: "quay.io/zagaos/observe-edge-dashboard"
    tag: "1a66ea3-20241217105748"
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 8080
  route:
    subdomain: edge-dashboard
    enabled: true

 

  dashboard:
    enabled: true
