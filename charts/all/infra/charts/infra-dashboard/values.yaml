infra-dashboard:

replicaCount: 1

image:
  repository: "quay.io/zagaos/zagaobserve-infra-dashboard"
  tag: "v9"
  pullPolicy: IfNotPresent
namespace: observability-edge

service:
  type: ClusterIP
  port: 8080

route:
  subdomain: infra-dashboard

dashboard:
  enabled: true
